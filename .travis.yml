language: bash

env:
  - SHUNIT_COLOR='always'

script:
  - make init
  - make test

os:
  - linux
  - osx

matrix:
  include:
    - os: linux
      script:
        - shellcheck *.sh
    - os: linux
      dist: trusty
